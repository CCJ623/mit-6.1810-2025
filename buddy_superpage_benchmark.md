# xv6 Buddy System & Superpage (大页) 性能测试报告

本报告记录了在 xv6 操作系统中实现的 **Buddy System (伙伴系统)** 物理内存分配器与 **Superpage (2MB 大页)** 虚拟内存映射机制的性能评测过程、测试方法、底层机制差异以及详实的数据分析。

---

## 1. 特性背景与机制概述

### 1.1 Buddy System (伙伴系统)
为了从根本上解决原生 xv6 物理内存分配器以单个 4KB 页面为单位进行单链表管理、无法分配大块连续物理内存（导致外部碎片化严重）的问题，我们实现了一个支持多阶层分配的 **Buddy System**：
- **分配单位**：支持阶数（Order）从 $0$（$4\text{ KB}$）到 $9$（$2\text{ MB}$）的物理内存块。
- **状态追踪**：采用 `kmem.order` 数组进行页的 Order 状态追踪，配合位图或状态数组实现伙伴块状态的 $O(1)$ 查询。
- **内存合并**：基于物理地址 XOR 位运算在 `kfree` 时自动寻找并递归合并相邻的空闲伙伴块（Buddy Coalescence），规避了外部碎片。

### 1.2 Superpage (2MB 大页)
在物理连续分配的基础上，利用 RISC-V Sv39 三级页表架构实现大页映射：
- **物理分配**：在 Eager (强) 模式下，当 `sbrk()` 申请的内存地址与大小均满足 2MB 对齐时，触发 `superAlloc()` 直接向内核申请 2MB（Order 9）的物理页。
- **映射优化**：不进入传统的 Level 0 页表，而是通过 `superWalk` 直接在三级页表的 Level 1 非叶子节点上配置映射（设置 `PTE_V` 并指向叶子物理地址），直接省去了第四级（Level 0）页表的分配与配置。

---

## 2. 评测方案设计 (Benchmark Setup)

### 2.1 控制变量测试组
1. **基准组 (Baseline)**：基于原生 `upstream/pgtbl` 分支，采用标准 4KB 小页进行动态内存扩展与分配。
2. **实验组 (Experimental)**：基于 `feature-superpage` 分支，启用 Buddy 分配器与 2MB 大页对齐映射。

### 2.2 压测程序 (`user/superbench.c`)
压测程序逻辑如下：
1. **地址对齐**：通过 `sbrk(0)` 探测堆边界，对齐至 2MB 物理边界以确保能成功触发大页条件。
2. **内存扩展**：申请 **$16\text{ MB}$** 的虚拟/物理内存。
3. **访存压测**：循环迭代 $50000$ 次，每次以 $4\text{ KB}$ 为步长（Stride）跨页跳跃读写该 $16\text{ MB}$ 内存，人为制造高强度的 TLB Miss。
4. **性能监控（内核埋点）**：在内核 `walk()` 与 `superWalk()` 中注入计数器，统计该次 `sbrk` 过程中执行的**软件页表遍历次数**。

---

## 3. 测试结果与数据分析

在 QEMU 模拟器（128M 物理内存，3 CPUs）环境下执行 `superbench` 的数据记录如下：

### 3.1 耗时对比 (Ticks)
*   **基准组 (Baseline)**：**22 ticks**
*   **实验组 (Superpage)**：**23 ticks**

> **关于耗时没有大幅下降的架构剖析**：
> QEMU 作为一个基于纯软件二进制翻译（TCG）的模拟器，其访存地址翻译是通过宿主机 C 语言哈希表（SoftMMU）模拟的。QEMU 内部并没有仿真物理 CPU 的硬件 TLB 延迟惩罚（在物理芯片上，一次 TLB Miss 会导致流水线挂起并执行 3 次物理访存，带来数百个时钟周期的惩罚）。
> 
> 在 QEMU 中，由于宿主机拥有巨大的 CPU 缓存，多级页表遍历的性能开销被抹平。实验组因为增加了 Buddy 分配器伙伴块拆分与合并的逻辑指令，在纯模拟器耗时上反而产生了 $1\text{ tick}$ 的额外软件指令开销。**在真实的物理 RISC-V 芯片上，大页通常会带来 $10\%\sim 30\%$ 甚至数倍的访存性能加速。**

### 3.2 页表遍历次数 (Page Table Walks) - 绝对指标
我们在内核系统调用 `sys_sbrk()` 建表前后的 `walk_count` 进行差值计算，得到了无法被模拟器效应掩盖的绝对元数据指标：

| 阶段 / 分配模式 | 基准组 (4KB 小页) | 实验组 (2MB 大页) | 提升效率 (降幅) |
| :--- | :--- | :--- | :--- |
| **分配 16MB 连续堆内存** | **4096 次** `walk()` | **8 次** `superWalk()` | **降低至 1/512** |

#### 数据分析与结论：
1. **基准组（小页）**：$16\text{ MB} / 4\text{ KB} = 4096$ 页。内核必须遍历 $4096$ 次页表，分配 $4096$ 个底层的 Level 0 页表项，并动态分配数十个二级/三级页表物理页来存放这些 PTE。
2. **实验组（大页）**：$16\text{ MB} / 2\text{ MB} = 8$ 页。内核只需调用 $8$ 次页表遍历，直接在 Level 1 中填入 8 个 PTE，完全消除了对这 8 个大页中包含的 $4096$ 个叶子节点 PTE 的处理。
3. **架构意义**：大页机制使得操作系统管理同样的 16MB 内存所需的**内核态遍历次数直接降到了 1/512**。极大地降低了内核建表元数据开销，减少了页表所占用的物理内存（Page Table Memory Overhead），且在硬件层面将 TLB 覆盖率提升了 512 倍，彻底避免了在大范围顺序或跳跃访存下的 TLB Thrashing。

---

## 4. 结论与简历亮点总结

1. **Buddy System 性能闭环**：验证了 Buddy 分配器能够稳定地支持大块连续物理内存的申请与归还，合并逻辑无死锁。
2. **Superpage 路径打通**：通过 `sbrk` 显式边界对齐，成功触发并打通了内核 `superAlloc()` 到 `mapSuperPages()` 的三级映射简化流程。
3. **核心亮点**：
   - 内核在建表时的软件开销缩减到 **1/512**。
   - 物理页表存储空间开销大比例节省。
   - 奠定了在真实芯片硬件上提升 TLB 命中率的坚实架构基础。
